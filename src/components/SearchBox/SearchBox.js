import React from 'react';

import './SearchBox.scss';

// Since this component is simple and static, there's no parent container for it.
function SearchBox() {
  return (
    <div className="searchbox">Searchbox here</div>
  );
}

export default SearchBox;
